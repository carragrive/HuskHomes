pluginManagement {
    repositories {
        gradlePluginPortal()
        maven { url 'https://repo.william278.net/releases' }
    }
}

rootProject.name = 'HuskHomesX'
include(
        'common',
        'bukkit',
        'paper',
)
